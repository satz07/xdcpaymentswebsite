var module_80589551936 = function() {
    var __hs_messages = {};
    i18n_getmessage = function() {
        return hs_i18n_getMessage(__hs_messages, hsVars.language, arguments)
    }
        ,
        i18n_getlanguage = function() {
            return hsVars.language
        }
        ,
        $(document).ready((function() {
                $(".header").on("click", ".header-menu-icon", (function() {
                        let nav_sibling = $(this).siblings(".header__navigation");
                        $(this).toggleClass("active"),
                            nav_sibling.slideToggle()
                    }
                )),
                    $(".has-submenu.menu-item").on("click", ".arrow.arrow-dropdown", (function() {
                            if ($("body").width() < 1025) {
                                let sibling = $(this).siblings("ul");
                                sibling.is(":visible") ? ($(this).removeClass("active"),
                                    sibling.slideUp()) : ($(".has-submenu.menu-item .arrow.arrow-dropdown").removeClass("active"),
                                    $(".has-submenu.menu-item ul.submenu.level-2").slideUp(),
                                    $(this).addClass("active"),
                                    sibling.slideDown())
                            }
                        }
                    )),
                    $(".submenu-wide-container-link").on("click mouseenter", (function(e) {
                            if ($("body").width() >= 1025) {
                                let $me = $(this)
                                    , $parent = $me.closest(".submenu-wide-container-parent")
                                    , $target = $parent.find('.submenu-wide-container-item[data-index="' + $me.attr("data-target") + '"]')
                                    , $layerParent = $parent.find(".submenu-wide-container");
                                $layerParent.removeClass("has-active"),
                                    $parent.find(".submenu-wide-container-link").removeClass("active"),
                                    $parent.find(".submenu-wide-container-item").removeClass("active"),
                                $target && $target.attr("data-items") > 0 && (e.preventDefault(),
                                    $target.addClass("active"),
                                    $me.addClass("active"),
                                    $layerParent.addClass("has-active"))
                            }
                        }
                    ))
            }
        ))
}();
