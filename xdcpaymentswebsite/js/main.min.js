!function() {
    window.NodeList && !NodeList.prototype.forEach && (NodeList.prototype.forEach = function(callback, thisArg) {
            thisArg = thisArg || window;
            for (var i = 0; i < this.length; i++)
                callback.call(thisArg, this[i], i, this)
        }
    );
    var callback, Nav = document.querySelector(".header__navigation"), navToggle = document.querySelector(".header__navigation--toggle"), closeToggle = document.querySelector(".header__close--toggle"), allElements = document.querySelectorAll(".header--element, .header--toggle");
    function showFocusOutline() {
        document.body.classList.add("enable-focus-styles"),
            document.body.classList.remove("disable-focus-styles")
    }
    function hideFocusOutline() {
        document.body.classList.add("disable-focus-styles"),
            document.body.classList.remove("enable-focus-styles")
    }
    function toggleNav() {
        Nav.classList.toggle("open"),
        navToggle && navToggle.classList.toggle("open")
    }
    function closeAll() {
        allElements.forEach((function(element) {
                element.classList.remove("hide", "open")
            }
        )),
        closeToggle && closeToggle.classList.remove("show")
    }
    callback = function() {
        document.body && (document.body.addEventListener("keydown", showFocusOutline),
            document.body.addEventListener("mousemove", hideFocusOutline),
            document.body.addEventListener("mousedown", hideFocusOutline),
            document.body.addEventListener("mouseup", hideFocusOutline),
        navToggle && navToggle.addEventListener("click", toggleNav),
        closeToggle && closeToggle.addEventListener("click", closeAll),
            $(".button").mousedown((function(e) {
                    var target = e.target
                        , rect = target.getBoundingClientRect()
                        , ripple = target.querySelector(".ripple");
                    $(ripple).remove(),
                        (ripple = document.createElement("span")).className = "ripple",
                        ripple.style.height = ripple.style.width = Math.max(rect.width, rect.height) + "px",
                        target.appendChild(ripple);
                    var top = e.pageY - rect.top - ripple.offsetHeight / 2 - document.body.scrollTop
                        , left = e.pageX - rect.left - ripple.offsetWidth / 2 - document.body.scrollLeft;
                    return ripple.style.top = top + "px",
                        ripple.style.left = left + "px",
                        !1
                }
            )))
    }
        ,
        ["interactive", "complete"].indexOf(document.readyState) >= 0 ? callback() : document.addEventListener("DOMContentLoaded", callback)
}();
//# sourceURL=https://cdn2.hubspot.net/hub/21152810/hub_generated/template_assets/69097112968/1649789228545/impel2020/js/main.js
